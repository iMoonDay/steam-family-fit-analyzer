# 更新日志

## 0.1.2

### 新增

- 新增价格模式切换，可在右上角菜单中切换。
- 价格模式支持“快速”和“中文名”两种：
  - 快速：批量读取原价，不补中文名，速度更快。
  - 中文名：逐个读取原价并补中文名，速度较慢。

### 优化

- 快速价格模式下会先批量读取原价。
- 批量结果无价格时，再单独查询确认免费或无价格状态。
- 两种价格模式使用独立缓存，避免数据互相污染。

### 说明

- 家庭共享支持性检测仍受 Steam 接口限制，目前无法使用 `categories` 批量查询。
- 快速价格模式只加速价格读取，不会加速家庭共享支持性检测。

## 0.1.1

### 新增

- 新增 HTTP 429 限流暂停机制。
- Steam 商店接口返回请求过快时，会立即暂停后续后台请求。
- 限流时显示“继续”和“限流检测”按钮。
- “继续”用于手动恢复未完成的统计或价格加载。
- “限流检测”用于检查 Steam 商店接口是否已经解除限流。

### 优化

- 限流后不再自动反复重试，避免持续触发 Steam 请求上限。
- 价格加载遇到限流时，不再把当前游戏误标记为 `N/A`。
- 支持家庭共享统计遇到限流时，会保留当前进度，稍后可继续。
