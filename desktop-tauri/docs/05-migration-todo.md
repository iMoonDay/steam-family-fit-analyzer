# 桌面端迁移 TODO

这份清单按当前桌面端实际状态整理。完成一项后把 `[ ]` 改成 `[x]`。

## 优先迁移

- [ ] 多账号对比视图
  - 迁移脚本里的账号游戏对比能力。
  - 覆盖独占、共有、独有新增、价格区间和质量统计。
  - 复用当前多账号分析结果，避免重新请求目标库。

- [x] 家庭库独立列表页
  - 增加完整家庭库列表入口。
  - 支持搜索、排序、贡献者展示和 Steam 客户端跳转。
  - 与当前结果页列表交互保持一致。

- [x] 家庭库封面图导出
  - 支持导出完整家庭库封面图。
  - 支持列数、排序和缩放设置。
  - 复用当前列表封面图导出逻辑。

- [ ] 原始返回数据窗口
  - 显示 Steam、Store、ITAD 等请求返回的原始数据。
  - 用于排查接口格式变化、鉴权失败和数据异常。
  - 默认只在需要时打开，不常驻主界面。

## 登录与凭据

- [ ] Cookie 登录实现
  - 使用 `sessionid` 和 `steamLoginSecure` 登录。
  - 从 `steamLoginSecure` 解析 SteamID64 和 access token。
  - 登录成功后写入现有登录缓存。

- [ ] 账号密码登录实现
  - 目前仅占位。
  - 需要确认 Steam 登录 API、二步验证和风控处理方式。
  - 如果实现成本或风控风险过高，可以保留占位或移除入口。

- [ ] 登录方式文案与状态收敛
  - 扫码、Cookie、账号密码的错误文案统一成用户能直接理解的短句。
  - 登录失效、刷新失败、凭据缺失保持单条状态提示。

## 可选补齐

- [ ] 自动 24 小时刷新家庭库
  - 从脚本迁移自动刷新开关。
  - 桌面端需要明确刷新触发时机：启动后、登录刷新后或后台定时。
  - 失败时只保留常驻登录页提示，不打断当前操作。

- [ ] Steam 页面侧边按钮能力评估
  - 这是脚本环境能力，桌面端不一定需要照搬。
  - 如果后续继续维护脚本或插件版，再单独处理。

- [ ] Community 主页自动填入目标账号
  - 脚本能在 Steam Community 资料页自动识别当前账号。
  - 桌面端没有页面上下文，除非做浏览器插件或外部协议入口，否则不迁移。

## 文档同步

- [ ] 更新桌面端 README
  - 替换过时的“当前未迁移”列表。
  - 补充扫码登录、登录缓存、配置目录、缓存目录和 Steam 客户端跳转说明。

- [ ] 更新使用指南
  - 补充登录页、配置页、历史分析和结果页的新布局。
  - 说明哪些数据存配置目录，哪些数据存缓存目录。

- [ ] 更新架构文档
  - 移除“后续再做 WebView 登录”的旧描述。
  - 补充当前 Steam 登录 API、刷新 token、登录缓存和自动获取 API Key 的链路。

## 已完成

- [x] Tauri 桌面壳和基础页面结构。
- [x] 账号输入解析：SteamID64、好友码、主页 URL、自定义 ID。
- [x] 目标账号公开游戏库分析。
- [x] 家庭库读取并参与新增、重复、相对新增计算。
- [x] 当前账号 owned appids 排除。
- [x] Steam Store 原价、本地化名、封面和家庭共享支持性读取。
- [x] ITAD 史低价模式。
- [x] 结果页表格/封面视图、搜索、排序和复制。
- [x] 提取通用游戏列表模块。
- [x] 单游戏刷新封面、批量重载封面。
- [x] 当前列表封面图导出。
- [x] 配置导入/导出。
- [x] 配置目录和缓存目录管理。
- [x] 登录页与扫码登录。
- [x] 登录缓存迁移到缓存目录。
- [x] 启动时读取登录缓存并显示头像。
- [x] 登录失效常驻提示和退出登录。
- [x] Steam Web API Key、家庭库 token、家庭组 ID 自动写入登录缓存。
- [x] 去除 Steam API、家庭库 token、SteamID64 手动填写入口。
- [x] ITAD API Key 保留在配置页。
- [x] 游戏和目标账号优先通过 Steam 客户端打开。
